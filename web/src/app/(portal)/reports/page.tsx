import { SectionCard } from "@/components/ui/SectionCard";

export default function ReportsPage() {
    return (
        <SectionCard
            title="Reports & Analytics"
            subtitle="Cross-policy outcomes, confidence bands, and decision drift"
        >
            <p className="text-sm text-[var(--muted)]">
                This canvas is staged for chart integration and report exports in the next phase.
            </p>
        </SectionCard>
    );
}
